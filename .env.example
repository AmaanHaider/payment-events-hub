# Copy this file to `.env` and fill in your real DATABASE_URL.
# `.env` is gitignored — never commit secrets.
#
# Replace YOUR_* placeholders with your Postgres user, password, host, port, and database name.
DATABASE_URL=postgresql+psycopg://YOUR_USER:YOUR_PASSWORD@localhost:5432/YOUR_DATABASE
